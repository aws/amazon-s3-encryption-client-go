# This workflow runs for every push to main
name: Push CI

on:
  push:
    branches:
      - main

jobs:
  push-ci-go-test:
    runs-on: ubuntu-latest
    steps:
    - name: Go Test
    # uses: ./.github/workflows/go-test.yml
      run: |
        - echo ${{ secrets.CI_AWS_ACCOUNT_ID }}